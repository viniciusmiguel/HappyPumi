# widget-template

A C# Pulumi template that provisions a `Widget` component backed by the fake-cloud (Pulumi `random`)
provider. Published to the HappyPumi registry and deployed from the console's Templates page via the
workflow runner. Targets `net9.0` to match the deployment executor image's .NET SDK.
