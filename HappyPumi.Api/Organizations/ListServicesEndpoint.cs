// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (do not regenerate over it).
#nullable enable

using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Organizations;

/// <summary>ListServices — GET /api/orgs/{org}/services. Lists the org's IDP services.</summary>
public sealed class ListServicesEndpoint(IServiceStore services) : Endpoint<ListServicesRequest, ListServicesResponse>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/services");
        AllowAnonymous();
        Description(b => b.WithTags("Organizations").WithSummary("ListServices").WithName("ListServices"));
    }

    public override async Task HandleAsync(ListServicesRequest req, CancellationToken ct)
    {
        var list = services.List(req.OrgName).Select(s => new Service
        {
            Name = s.Name,
            Description = s.Description,
            OrganizationName = req.OrgName,
            Created = s.Created,
        }).ToList();
        await Send.OkAsync(new ListServicesResponse { Services = list, ContinuationToken = null }, ct);
    }
}
