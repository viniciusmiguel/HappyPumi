// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (do not regenerate over it).
#nullable enable

using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Organizations;

/// <summary>ListAuditLogEventsHandlerV1 — GET /api/orgs/{org}/auditlogs. Returns recorded audit events (ADR-0010).</summary>
public sealed class ListAuditLogEventsHandlerV1Endpoint(IAuditLog audit)
    : Endpoint<ListAuditLogEventsHandlerV1Request, ResponseAuditLogs>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/auditlogs");
        AllowAnonymous();
        Description(b => b.WithTags("Organizations").WithSummary("ListAuditLogEventsHandlerV1").WithName("ListAuditLogEventsHandlerV1"));
    }

    public async override Task HandleAsync(ListAuditLogEventsHandlerV1Request req, CancellationToken ct)
    {
        var events = audit.List(req.OrgName).Select(a => new AuditLogEvent
        {
            Event = a.Event,
            Description = a.Description,
            ActorName = a.ActorName,
            SourceIp = a.SourceIp,
            Timestamp = new System.DateTimeOffset(a.Timestamp, System.TimeSpan.Zero).ToUnixTimeSeconds(),
            User = new UserInfo { GithubLogin = a.ActorName, Name = a.ActorName },
        }).ToList();
        await Send.OkAsync(new ResponseAuditLogs { AuditLogEvents = events, ContinuationToken = null }, ct);
    }
}
