// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (do not regenerate over it).
#nullable enable

using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Organizations;

/// <summary>ListTeams — GET /api/orgs/{org}/teams. Lists the org's teams (RBAC).</summary>
public sealed class ListTeamsEndpoint(IIdentityStore identity) : Endpoint<ListTeamsRequest, ListTeamsResponse>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/teams");
        AllowAnonymous();
        Description(b => b.WithTags("Organizations").WithSummary("ListTeams").WithName("ListTeams"));
    }

    public override async Task HandleAsync(ListTeamsRequest req, CancellationToken ct)
    {
        var teams = identity.ListTeams(req.OrgName).Select(TeamMapper.ToTeam).ToList();
        await Send.OkAsync(new ListTeamsResponse { Teams = teams }, ct);
    }
}
