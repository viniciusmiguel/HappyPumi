// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (do not regenerate over it).
#nullable enable

using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Organizations;

/// <summary>GetTeam — GET /api/orgs/{org}/teams/{team}. Returns a team with its members and role ids.</summary>
public sealed class GetTeamEndpoint(IIdentityStore identity) : Endpoint<GetTeamRequest, Team>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/teams/{teamName}");
        AllowAnonymous();
        Description(b => b.WithTags("Organizations").WithSummary("GetTeam").WithName("GetTeam"));
    }

    public override async Task HandleAsync(GetTeamRequest req, CancellationToken ct)
    {
        var team = identity.GetTeam(req.OrgName, req.TeamName);
        if (team is null)
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        await Send.OkAsync(TeamMapper.ToTeam(team), ct);
    }
}
