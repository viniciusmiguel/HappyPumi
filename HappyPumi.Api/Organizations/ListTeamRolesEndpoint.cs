// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (do not regenerate over it).
#nullable enable

using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Organizations;

/// <summary>ListTeamRoles — GET /api/orgs/{org}/teams/{teamName}/roles. The fine-grained roles a team holds.</summary>
public sealed class ListTeamRolesEndpoint(IIdentityStore identity) : Endpoint<ListTeamRolesRequest, ListTeamRolesResponse>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/teams/{teamName}/roles");
        AllowAnonymous();
        Description(b => b.WithTags("Organizations").WithSummary("ListTeamRoles").WithName("ListTeamRoles"));
    }

    public override async Task HandleAsync(ListTeamRolesRequest req, CancellationToken ct)
    {
        var team = identity.GetTeam(req.OrgName, req.TeamName);
        if (team is null)
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        var roles = team.RoleIds
            .Select(id => identity.GetRole(req.OrgName, id))
            .Where(role => role is not null)
            .Select(role => IdentityMapper.ToRecord(role!))
            .ToList();
        await Send.OkAsync(new ListTeamRolesResponse { Roles = roles }, ct);
    }
}
