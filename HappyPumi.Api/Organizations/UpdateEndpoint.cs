// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Organizations;

/// <summary>
/// UpdateChangeRequest — sets the description and records a <c>description_updated</c> timeline event.
/// </summary>
public sealed class UpdateEndpoint(IChangeRequestStore changeRequests, IChangeRequestEventStore events)
    : Endpoint<UpdateRequest>
{
    public override void Configure()
    {
        Patch("/api/change-requests/{orgName}/{changeRequestID}");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Organizations")
            .WithSummary("UpdateChangeRequest")
            .WithDescription("Updates a change request's metadata. Currently only the description field can be modified after creation.")
            .WithName("Update")
        );
    }

    public override async Task HandleAsync(UpdateRequest req, CancellationToken ct)
    {
        var cr = changeRequests.Update(req.OrgName, req.ChangeRequestId, c => c.Description = req.Body.Description);
        if (cr is null)
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        var actor = User.Identity?.Name ?? "happypumi";
        events.Append(ChangeRequestActivity.Event(cr, "description_updated", actor, comment: req.Body.Description));
        await Send.NoContentAsync(ct);
    }
}
