// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand. Bodyless
// (EndpointWithoutRequest) because the generated request's Body resolves to the self-referential
// endpoint request type (a generator quirk), so the JSON body is parsed directly.
#nullable enable

using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.Endpoints.Organizations;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Workflows;

/// <summary>
/// PatchOrgAgentPool — updates a pool's name/description (omitted fields unchanged); 404 when absent.
/// </summary>
public sealed class PatchOrgAgentPoolEndpoint(IAgentPoolStore pools) : EndpointWithoutRequest<PatchOrgAgentPoolResponse>
{
    public override void Configure()
    {
        Patch("/api/orgs/{orgName}/agent-pools/{poolId}");
        AllowAnonymous();
        Description(b => b.WithTags("Workflows").WithSummary("PatchOrgAgentPool").WithName("PatchOrgAgentPool"));
    }

    public override async Task HandleAsync(CancellationToken ct)
    {
        var org = Route<string>("orgName")!;
        var poolId = Route<string>("poolId")!;
        var body = await PlatformBody.ReadAsync(HttpContext, ct);

        var pool = pools.UpdatePool(org, poolId, body.Str("name"), body.Str("description"));
        if (pool is null)
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        await Send.OkAsync(new PatchOrgAgentPoolResponse { Name = pool.Name, Description = pool.Description }, ct);
    }
}
