// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using System.Linq;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Workflows;

/// <summary>
/// ListOrgAgentPool — the org's agent pools (without their secret tokens; tokens are returned only at creation).
/// </summary>
public sealed class ListOrgAgentPoolEndpoint(IAgentPoolStore pools)
    : Endpoint<ListOrgAgentPoolRequest, ListAgentPoolsResponse>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/agent-pools");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Workflows")
            .WithSummary("ListOrgAgentPool")
            .WithDescription("Returns all agent pools configured for an organization. Agent pools enable self-hosted deployment agents, allowing organizations to run Pulumi Deployments on their own infrastructure for accessing private networks, meeting compliance requirements, or using custom execution environments. Each pool in the response includes its ID, name, and configuration details.")
            .WithName("ListOrgAgentPool")
        );
    }

    public override async Task HandleAsync(ListOrgAgentPoolRequest req, CancellationToken ct)
    {
        var pools_ = pools.ListPools(req.OrgName).Select(p => new AgentPool
        {
            Id = p.Id,
            Description = p.Description,
            Created = new System.DateTimeOffset(p.Created, System.TimeSpan.Zero).ToUnixTimeSeconds(),
        }).ToList();
        await Send.OkAsync(new ListAgentPoolsResponse { AgentPools = pools_ }, ct);
    }
}
