// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Stacks;

/// <summary>
/// ExportStack
/// </summary>
public sealed class ExportStackEndpoint(IStackStore stacks) : Endpoint<ExportStackRequest, AppUntypedDeployment>
{
    public override void Configure()
    {
        Get("/api/stacks/{orgName}/{projectName}/{stackName}/export");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Stacks")
            .WithSummary("ExportStack")
            .WithDescription("Exports the current, complete state of the stack as an untyped deployment object. The response includes the deployment version, manifest (containing timestamps and plugin information), secrets providers configuration, and the full array of resources with their URNs, types, inputs, outputs, and dependency information. This endpoint is commonly used for stack state backup, migration between backends, or programmatic inspection of resource states.")
            .WithName("ExportStack")
        );
    }

    public async override Task HandleAsync(ExportStackRequest req, CancellationToken ct)
    {
        // The engine reads this as the base snapshot at the start of every update, so an unknown stack
        // is 404 but a known-but-undeployed one returns the empty (valid) deployment, not 404.
        var stack = stacks.Find(new StackCoordinates(req.OrgName, req.ProjectName, req.StackName));
        if (stack is null)
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        await Send.OkAsync(stack.Deployment ?? DeploymentFactory.Empty(), ct);
    }
}
