// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (do not regenerate over it).
#nullable enable

using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Stacks;

/// <summary>
/// GetStackPolicyPacks — the Pulumi engine calls this during an update to learn which policy packs it must
/// download and enforce. We apply the org's default policy group ("default-policy-group") to every stack:
/// each enabled pack's newest published version is returned with a download URL (packLocation) the engine
/// fetches to materialize the pack.
/// </summary>
public sealed class GetStackPolicyPacksEndpoint(IPolicyStore policy)
    : Endpoint<GetStackPolicyPacksRequest, AppGetStackPolicyPacksResponse>
{
    public override void Configure()
    {
        Get("/api/stacks/{orgName}/{projectName}/{stackName}/policypacks");
        AllowAnonymous();
        Description(b => b
            .WithTags("Stacks")
            .WithSummary("GetStackPolicyPacks")
            .WithName("GetStackPolicyPacks"));
    }

    public override async Task HandleAsync(GetStackPolicyPacksRequest req, CancellationToken ct)
    {
        var baseUrl = $"{HttpContext.Request.Scheme}://{HttpContext.Request.Host}";
        var required = PolicyEnforcement.RequiredPolicies(policy, req.OrgName, baseUrl);
        await Send.OkAsync(new AppGetStackPolicyPacksResponse { RequiredPolicies = required }, ct);
    }
}
