// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Stacks;

/// <summary>
/// RenewUpdateLease
/// </summary>
public sealed class RenewUpdateLeaseDestroyEndpoint(UpdateLifecycle lifecycle) : Endpoint<RenewUpdateLeaseDestroyRequest, AppRenewUpdateLeaseResponse>
{
    public override void Configure()
    {
        Post("/api/stacks/{orgName}/{projectName}/{stackName}/destroy/{updateID}/renew_lease");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Stacks")
            .WithSummary("RenewUpdateLease")
            .WithDescription("Renews the lease for a service-managed update that is currently in progress. Leases prevent concurrent operations on the same stack and must be periodically renewed to indicate the update is still active. The renewal duration must be between 0 and 300 seconds. Returns 409 if the update is not currently in progress.")
            .WithName("RenewUpdateLeaseDestroy")
        );
    }

    public async override Task HandleAsync(RenewUpdateLeaseDestroyRequest req, CancellationToken ct)
    {
        var update = lifecycle.Find(req.UpdateId);
        if (update is null)
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        // Leases never expire in this in-memory backend; hand back a 5-minute horizon to satisfy the
        // CLI's renewal loop.
        await Send.OkAsync(new AppRenewUpdateLeaseResponse
        {
            Token = update.Token,
            TokenExpiration = DateTimeOffset.UtcNow.AddSeconds(300).ToUnixTimeSeconds(),
        }, ct);
    }
}
