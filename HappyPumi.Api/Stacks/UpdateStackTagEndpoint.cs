// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Stacks;

/// <summary>
/// UpdateStackTag
/// </summary>
public sealed class UpdateStackTagEndpoint(IStackStore stacks) : Endpoint<UpdateStackTagRequest>
{
    public override void Configure()
    {
        Patch("/api/stacks/{orgName}/{projectName}/{stackName}/tags/{tagName}");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Stacks")
            .WithSummary("UpdateStackTag")
            .WithDescription("Updates the value of an existing tag on the specified stack. The tag is identified by its name in the URL path. Built-in tags (those automatically managed by the Pulumi CLI, such as project name tags) cannot be updated and will return a 400 error. Returns 404 if the specified tag does not exist on the stack.")
            .WithName("UpdateStackTag")
        );
    }

    public async override Task HandleAsync(UpdateStackTagRequest req, CancellationToken ct)
    {
        // Updating a tag only makes sense for one that exists; an unknown stack or tag is 404 (mirrors DeleteStackTag).
        var coords = new StackCoordinates(req.OrgName, req.ProjectName, req.StackName);
        var stack = stacks.Find(coords);
        if (stack is null || !stack.Tags.ContainsKey(req.TagName))
        {
            await Send.NotFoundAsync(ct);
            return;
        }

        stacks.SetTag(coords, req.TagName, req.Body.Value);
        await Send.NoContentAsync(ct);
    }
}
