// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Deployments;

/// <summary>
/// ListOrgDeployments
/// </summary>
public sealed class ListOrgDeploymentsEndpoint(IDeploymentStore deployments) : Endpoint<ListOrgDeploymentsRequest, ListDeploymentResponseV2>
{
    public override void Configure()
    {
        Get("/api/orgs/{orgName}/deployments");
        Permissions("deployments:read");
        Description(b => b
            .WithTags("Deployments")
            .WithSummary("ListOrgDeployments")
            .WithDescription("Returns a paginated list of all Pulumi Deployments executions across an organization, filtered to only include deployments for stacks the requesting user has access to. The response includes each deployment's ID, status, version, creation time, the requesting user, project and stack names, Pulumi operation type, job details, and associated update results. Use 'page' (minimum 1, default 1) and 'pageSize' (1-100, default 10) for pagination, 'sort' to specify the sort field, and 'asc' to control sort direction (default descending). The response also includes the total count of matching deployments.")
            .WithName("ListOrgDeployments")
        );
    }

    public override async Task HandleAsync(ListOrgDeploymentsRequest req, CancellationToken ct)
    {
        var all = deployments.ListByOrg(req.OrgName);
        var pageSize = req.PageSize is > 0 and <= 100 ? (int)req.PageSize.Value : 10;
        var page = req.Page is > 0 ? (int)req.Page.Value : 1;
        var snapshots = all.Skip((page - 1) * pageSize).Take(pageSize)
            .Select(DeploymentMapper.ToSnapshot).ToList();
        await Send.OkAsync(new ListDeploymentResponseV2
        {
            Deployments = snapshots, ItemsPerPage = pageSize, Total = all.Count,
        }, ct);
    }
}
