// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using System.Linq;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;
using HappyPumi.Api.Secrets;

namespace HappyPumi.Api.Endpoints.Deployments;

/// <summary>
/// PatchDeploymentSettings
/// </summary>
public sealed class PatchDeploymentSettingsEndpoint(IDeploymentStore deployments) : Endpoint<PatchDeploymentSettingsRequest, DeploymentSettings>
{
    public override void Configure()
    {
        Post("/api/stacks/{orgName}/{projectName}/{stackName}/deployments/settings");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Deployments")
            .WithSummary("PatchDeploymentSettings")
            .WithDescription("Creates or updates Pulumi Deployments settings for a stack using a merge operation. If no settings exist, they are created. If settings already exist, the request body is merged with the current settings using the following logic: for each property, start with the current value, remove it if the patch specifies null, or merge the new non-null value with the existing one. Non-object properties (strings, numbers, booleans) are replaced entirely. Settings include source context (git repository URL, branch, directory), operation context (environment variables, pre-run commands, OIDC configuration), executor context, GitHub integration settings, and cache options. Requires a Team Growth or higher subscription. Cannot be used to configure Pulumi Deployments for Terraform stacks.")
            .WithName("PatchDeploymentSettings")
        );
    }

    public async override Task HandleAsync(PatchDeploymentSettingsRequest req, CancellationToken ct)
    {
        var settings = deployments.GetSettings(new StackCoordinates(req.OrgName, req.ProjectName, req.StackName)) ?? new DeploymentSettings();
        settings.AgentPoolId = req.Body?.AgentPoolId ?? settings.AgentPoolId;
        settings.Tag = req.Body?.Tag ?? settings.Tag;
        settings.Source ??= "git";
        deployments.SetSettings(new StackCoordinates(req.OrgName, req.ProjectName, req.StackName), settings);
        await Send.OkAsync(settings, ct);
    }
}
