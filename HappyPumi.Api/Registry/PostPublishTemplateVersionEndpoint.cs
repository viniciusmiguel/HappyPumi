// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.Contracts;

namespace HappyPumi.Api.Endpoints.Registry;

/// <summary>
/// PostPublishTemplateVersion
/// </summary>
public sealed class PostPublishTemplateVersionEndpoint(HappyPumi.Api.State.ITemplateRegistry registry) : Endpoint<PostPublishTemplateVersionRequest, StartTemplatePublishResponse>
{
    public override void Configure()
    {
        Post("/api/registry/templates/{source}/{publisher}/{name}/versions");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Registry")
            .WithSummary("PostPublishTemplateVersion")
            .WithDescription("Initiates the first step of a two-phase template version publish workflow. This creates a publish transaction and returns an operationID along with a pre-signed upload URL for the template archive. The source must be 'private'. The caller must upload the template archive (a gzip-compressed tar file containing a root-level Pulumi.yaml with a template section, and optionally a README.md) to the provided URL, then call PostPublishTemplateVersionComplete with the operationID to finalize the publish. The request body must include the semantic version to publish. Returns 202 Accepted with the operation details, or 404 if the source does not exist.")
            .WithName("PostPublishTemplateVersion")
        );
    }

    public async override Task HandleAsync(PostPublishTemplateVersionRequest req, CancellationToken ct)
    {
        if (string.IsNullOrWhiteSpace(req.Body?.Version))
        {
            AddError("'version' is required.", "version");
            await Send.ErrorsAsync(400, ct);
            return;
        }

        var coords = new HappyPumi.Api.State.TemplateCoordinates(req.Source, req.Publisher, req.Name);
        registry.StartPublish(coords, req.Body.Version);
        var uploads = HappyPumi.Api.State.RegistryMapper.TemplateUploadUrls(coords, req.Body.Version);
        // The CLI PUTs the archive directly to this URL, so it must be absolute (rooted at this host).
        uploads.Archive = $"{HttpContext.Request.Scheme}://{HttpContext.Request.Host}" + uploads.Archive;
        await Send.OkAsync(new StartTemplatePublishResponse
        {
            OperationId = System.Guid.NewGuid().ToString(),
            UploadUrLs = uploads,
        }, ct);
    }
}
