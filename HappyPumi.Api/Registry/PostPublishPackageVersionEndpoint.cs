// <auto-generated />
// This file was generated by PulumiApiGenerator. Do not edit by hand.
#nullable enable

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using System;
using FastEndpoints;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.Registry;

/// <summary>
/// PostPublishPackageVersion
/// </summary>
public sealed class PostPublishPackageVersionEndpoint(IPackageRegistry registry) : Endpoint<PostPublishPackageVersionRequest, StartPackagePublishResponse>
{
    public override void Configure()
    {
        Post("/api/registry/packages/{source}/{publisher}/{name}/versions");
        AllowAnonymous(); // TODO: replace with your auth policy (e.g. Roles(...), Policies(...))
        Description(b => b
            .WithTags("Registry")
            .WithSummary("PostPublishPackageVersion")
            .WithDescription("Initiates the first step of a two-phase package version publish workflow. This creates a publish transaction and returns an operationID along with pre-signed upload URLs for the package artifacts (schema, index, and installationConfiguration). The caller must upload all required artifacts to the provided URLs and then call the PostPublishPackageVersionComplete endpoint with the operationID to finalize the publish. The request body must include the semantic version to publish. Returns 202 Accepted with the operation details, 404 if the source does not exist, or 409 if the specified version already exists.")
            .WithName("PostPublishPackageVersion")
        );
    }

    public async override Task HandleAsync(PostPublishPackageVersionRequest req, CancellationToken ct)
    {
        if (string.IsNullOrWhiteSpace(req.Body?.Version))
        {
            AddError("'version' is required.", "version");
            await Send.ErrorsAsync(400, ct);
            return;
        }

        // Phase one of the publish handshake: register the (unpublished) version and hand back upload URLs.
        // The CLI PUTs each artifact directly to these URLs, so they must be absolute (rooted at this host).
        var coords = new PackageCoordinates(req.Source, req.Publisher, req.Name);
        registry.StartPublish(coords, req.Body.Version, req.Body.PublishedAt);
        var uploads = RegistryMapper.UploadUrls(coords, req.Body.Version);
        var baseUrl = $"{HttpContext.Request.Scheme}://{HttpContext.Request.Host}";
        uploads.Index = baseUrl + uploads.Index;
        uploads.Schema = baseUrl + uploads.Schema;
        uploads.InstallationConfiguration = baseUrl + uploads.InstallationConfiguration;
        await Send.OkAsync(new StartPackagePublishResponse
        {
            OperationId = Guid.NewGuid().ToString(),
            UploadUrLs = uploads,
        }, ct);
    }
}
