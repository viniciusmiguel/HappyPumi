// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand. Bodyless because the
// generated request type's Body is self-referential (a generator quirk); the setup body is advisory.
#nullable enable

using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.CloudSetup;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.CloudSetup;

/// <summary>AzureSetup — records that the org has set up the Azure cloud provider (connected-account row).</summary>
public sealed class AzureSetupEndpoint(IConnectedCloudAccountStore store) : EndpointWithoutRequest<CloudSetupResult>
{
    public override void Configure()
    {
        Post("/api/esc/cloudsetup/{orgName}/oauth/azure/setup");
        AllowAnonymous();
        Description(b => b
            .WithTags("CloudSetup")
            .WithSummary("AzureSetup")
            .WithDescription("Sets up Azure infrastructure and ESC environments using OAuth credentials")
            .WithName("AzureSetup")
        );
    }

    public override Task HandleAsync(CancellationToken ct)
    {
        var org = Route<string>("orgName")!;
        return Send.OkAsync(CloudSetupBody.RecordSetup(store, org, "azure", "Azure cloud setup recorded"), ct);
    }
}
