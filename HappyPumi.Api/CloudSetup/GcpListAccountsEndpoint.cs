// Originally generated by PulumiApiGenerator; HandleAsync implemented by hand (ctor injection only).
#nullable enable

using System.Threading;
using System.Threading.Tasks;
using FastEndpoints;
using HappyPumi.Api.CloudSetup;
using HappyPumi.Api.Contracts;
using HappyPumi.Api.State;

namespace HappyPumi.Api.Endpoints.CloudSetup;

/// <summary>GCPListAccounts — returns the org's stored GCP connected projects (empty when none).</summary>
public sealed class GcpListAccountsEndpoint(IConnectedCloudAccountStore store)
    : Endpoint<GcpListAccountsRequest, ListCloudAccountsResponse>
{
    public override void Configure()
    {
        Get("/api/esc/cloudsetup/{orgName}/oauth/gcp/accounts");
        AllowAnonymous();
        Description(b => b
            .WithTags("CloudSetup")
            .WithSummary("GCPListAccounts")
            .WithDescription("Lists GCP projects accessible with the provided oauth session")
            .WithName("GcpListAccounts")
        );
    }

    public override Task HandleAsync(GcpListAccountsRequest req, CancellationToken ct)
        => Send.OkAsync(CloudAccountMapper.ToResponse(store.List(req.OrgName, "gcp")), ct);
}
